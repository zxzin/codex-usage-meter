{
  "name": "token-meter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "token-meter": "./scripts/launch.mjs",
    "codex-usage-meter": "./scripts/launch.mjs"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "tsc && vite build",
    "launch": "node scripts/launch.mjs",
    "install:claude": "node scripts/install-claude.mjs",
    "claude:install": "node scripts/install-claude.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 1420",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "tauri:build:mac": "tauri build --bundles app,dmg",
    "tauri:build:windows": "tauri build --bundles nsis,msi"
  },
  "dependencies": {
    "@tauri-apps/api": "2.11.1",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.3",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.3",
    "typescript": "^6.0.3",
    "vite": "^8.1.0"
  }
}
